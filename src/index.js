import app from './app.js' // Nueva froma de exportar
// import routes from './routes/ejemplo.rutas.js' // Traer las rutas desde la carpeta rutas
// import {getConne} from "./database/connection.js" // LLamar conexión 
// getConne() // Probar getConne solo llamar la constante o funcion
app.listen(3000)
// app.use(routes) // Probar rutas






